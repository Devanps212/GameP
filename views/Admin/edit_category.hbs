
<!DOCTYPE html>
<html>
<head>
    <title>Edit Category - ADMIN</title>
    <style>
        body {
            background-color: #f2f2f2; /* Set the background color for the entire body */
            font-family: Arial, sans-serif;
            margin: 0;
        }

        .card {
            max-width: 400px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-top: 17px;
        }

        h1 {
            color: #333;
            text-align: center;
        }

        form {
            margin-top: 20px;
        }

        label {
            display: block;
            margin-bottom: 6px;
            color: #555;
        }

        input[type="text"] {
            width: 95%;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
            margin-top: 7px;
            box-shadow: inset 0px 0px 9px -5px black;
        }

        button[type="submit"] {
            background-color: #007bff;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            width: 100%; /* Make the button width 100% of the container */
            margin-top: 15px;
        }

        button[type="submit"]:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="card">
        <h1>Edit Category</h1>
        <form action="/admin/updateCategory/{{data._id}}" method="post">
            <label for="categoryName">Category Name:</label>
            <input class="press" type="text" id="categoryName" name="updatedname" value="{{data.name}}" required>
            <br>
            <button type="submit">Update Category</button>
        </form>
    </div>
</body>
</html>
